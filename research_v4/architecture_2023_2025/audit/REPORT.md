# Независимая проверка архитектурного сравнения

**PASS: 207 проверок, 0 ошибок.** Обучения, вызовов API и изменений исходных моделей не выполнялось. Проверка воспроизводится скриптом `verify.py`; машинный результат и SHA-256 входных артефактов сохранены в `verification.json`.

## Главный результат подтверждён

На общей выборке 727 дат KZT за 2023–2025 годы, с политикой `cadence85_cd2`:

| Модель FULL33, обучение только KZT | Сигналы / попадания | Lift | Brier | Недель с 1–2 сигналами |
| --- | --- | --- | --- | --- |
| TabM, заранее сохранённый seed 2 | 143 / 61 | 1,583809 | 0,184264 | 117/152 = 76,97% |
| Сопоставимый HGB | 152 / 69 | 1,617255 | 0,179845 | 121/152 = 79,61% |

Независимый парный месячный bootstrap воспроизвёл интервалы TabM−HGB: lift **[−0,225923; +0,161104]**, Brier **[−0,001518; +0,010524]**, относительный forward-эффект **[−12,008327; +11,149634] б.п.** Превосходство TabM в этой паре не установлено.

## Что проверено

- Все 18 локальных пар «модель–год»: общий обучающий fingerprint внутри feature family, область KZT, фактическое созревание пятой будущей метки до границ train/calibration/test и внутренней проверки эпох. Fitted median и quantile statistics независимо рассчитаны из обучающего окна без вызова `fit`; совпадение подтверждено. HGB использует сохранённый preprocessor соответствующего TabM seed 2 и заявленные фиксированные параметры.
- Сохранённые веса всех 18 локальных моделей воспроизвели 13 176 сырых прогнозов validation/history/test **точно, максимальная разность 0**. Незрелые исходы history замаскированы. Равновесный ансамбль проверен отдельно.
- Для трёх переиспользованных моделей 2025 года совпали исходные веса, preprocessor, metadata, fingerprint обучающих признаков и все 242 тестовых сырых прогноза каждой модели; максимальная разность 0. Это точное совпадение сырых прогнозов; новая общая float64-калибровка отдельно и корректно описана в основном отчёте.
- Все итоговые модели и политики сопоставлены с историческими V3 ключами date/corridor, метками и forward-исходами. Локальное обучение и pooled-обучение обозначены раздельно. Историческая V3 — системный контроль, а не изолированный эффект архитектуры.
- Все строки `summary.csv` воспроизведены независимой формулой. Lift использует ожидаемое число попаданий по ячейкам год×коридор; utility вычитает средний forward в той же ячейке. Годовые lift не усредняются. Недели считаются внутри каждого года: **152**, а не ошибочные 155 при непрерывном диапазоне трёх лет.
- Все **20 парных интервалов** воспроизведены отдельной реализацией с общими календарными месяцами, стратификацией по году и пересчётом базовых частот/средних в каждой перевыборке. Для каждого интервала и каждой из трёх метрик все **10 000** итоговых перевыборок конечны; доля nonfinite — **0%**, включая fixed threshold 0,5. Диагностика сохранена в `bootstrap_finite_draws.json`.
- Из JSON-коэффициентов калибровки и независимого последовательного алгоритма контактов воспроизведены 58 887 строк политик: максимальная разность вероятностей 0, маски контактов и начальные состояния совпали. Подбор Platt/порогов дополнительно проверен чтением кода: используются только зрелые метки предыдущего калибровочного года.
- `local.py`, `pooled/experiment.py`, `evaluate.py` и основной `REPORT.md` прочитаны. Ошибок чисел или доказуемых завышений вывода не найдено. Дополнительный аудит pooled-checkpoints расположен в `../pooled/independent_review.md` и не выдаётся за выполненный этим скриптом повторный replay.

## Границы вывода

Это ретроспективное сравнение фиксированных рецептов: TabM имеет внутренний подбор эпох, HGB фиксированное число деревьев; одинаковый бюджет поиска гиперпараметров не исследован. Рецепт и seed ранее выбирались с использованием 2025/2026. Интервалы условны на готовых моделях и не учитывают повторное обучение или множественный отбор. Coverage ниже 85% в 2023/2024 не скрыта. Forward-эффект относится к справочному FX-исходу и не является доказанной прибылью исполнения банковской сделки.

Для нового запуска с изменёнными входами следует использовать новую папку checkpoints или предварительно проверять полный fingerprint: существующие механизмы загрузки ориентированы на неизменные артефакты текущего запуска. Для проверенного пакета hashes, параметры и обучающие fingerprints совпали; открытых блокирующих замечаний нет.

Все записи этой проверки ограничены папкой `audit/`; `final_solution` и запечатанный `final_sprint` не изменялись.
