# Two scenarios and a bounded CLOSING model PoC

The case specifies two different truth conditions: NOW means current R[t] is no worse than every next h observation, while CLOSING means R[t+h]>R[t]. R is RUB per unit of recipient currency; lower is preferable for RUB→KZT remittance. Both use h=5 effective CBR observations here, tau=0. CLOSING is an endpoint directional label, not NOW, not a claim that today's quote is a local low, and not bank execution P&L.

Fixed experiment before fit: four fits, two cutoffs (2025-01-01 and2026-01-01), two source arms (long120m Treasury-only; long120m Treasury+Halyk). Both arms use the existing pooled HGB120/depth2 and KZT40-stump residual adaptation with prior KZT validation shrink weight grid [0,.25,.5,.75,1]. Treasury frozen lag7 features begin2020; older rows remain missing. Halyk BANK SELL RUB is an external feature with effective date+1 assumption, not the target and not the bank BUY side required for remittance. No OXR arm or added parameter search.

Existing annual purged splits, all-corridor training, prior12m KZT calibration, KZT test. Label date is independently the fifth future CBR observation. CLOSING target is strict R[t+5]>R[t], with full missing future windows excluded, zero-tolerance equality a failure. Preserve original NOW label separately for audit, never substitute it into CLOSING metrics. Preserve mean-future forward_bps and separately endpoint_bps. Fixed weights/calibration/policy are stressed by providing Halyk lag2 instead of lag1 only after cutoff; prior history remains normal.

Save test scores and complete prior12m history scores. History target/closing_target/now_target/forward_bps/endpoint_bps are blank wherever the label is not mature strictly before cutoff. Export `calibration_eligible` and `label_available_date`. Save ret1/pr60/rub_per_unit and recent-low context for causal factual-copy gates, computed from past observations only. Root's common combined policy will use these exports; its target is >=85% weeks with1–2 combined signals and separate scenario lift>=1.3. Those targets are acceptance checks, not claimed results of this small PoC.

Standalone diagnostic policy retains existing prior-validation threshold selection and cooldown3 CBR sessions, using the CLOSING label. Report all-market Brier against prior-validation prevalence, CLOSING candidate hit/lift, average endpoint and mean-forward bps relative to same-year KZT baseline, and all-observed-week cadence. Never average NOW and CLOSING hit labels into an unqualified combined lift. Root owns final joint policy and threshold selection.

Local stdlib adapter must carry scenario/target contract, source known-at, separate past baseline rates and thresholds, factual evidence, shared cap and dedup state. Compare normalized past-relative expected quality, not raw probabilities from events with different prevalence. At most one scenario is chosen per contact; rejected/missing/future input fails closed. Internal forecasts do not become client promises: public copy states historical/present facts only. Full final_solution remains unchanged.
