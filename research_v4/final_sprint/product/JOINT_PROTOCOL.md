# Fixed joint NOW/CLOSING follow-up

This joint evaluation is requested after individual CLOSING and root NOW candidates have been inspected; it is retrospective, not a new pristine holdout. Read the current NOW champion and policy from root selection.json (its ranking explicitly uses2026). Freeze that snapshot for the joint run. The CLI accepts arbitrary NOW prediction/history paths and records their SHA. Freeze CLOSING to `closing_treasury_halyk_shrink120m`; fit the same selected common-policy rule on its previous-year matured validation only. No additional joint test-threshold search. Initial requested HGB was replaced by the actual root selection before any joint evaluation.

Evaluate both 2025 and2026, normal and bank_delayed scores, on exact shared date/corridor keys. Four fixed schedules: NOW-only; joint; joint with CLOSING ret1>0 fact gate; joint with CLOSING ret1>0 and NOW(pr60<=.2 orret1<0) fact gates. Primary joint retains both model verdict types without assuming that a low-rank fact is a mandatory gate for a learned NOW event. Optional stronger copy gates are sensitivity checks, not selected using test results.

Joint scheduler permits at most two contacts per calendar week and the shared CBR-session cooldown specified by the selected NOW policy (two or three sessions). Thresholds remain separate by head. If both qualify on a day, rank probability divided by that head's prior-validation event prevalence; exact ties prefer NOW. Replay the same combined policy on all prior12m historical scores to derive carry state; history tail outcomes are not used. At most one contact per market date/corridor; all model rows correspond to the same market path.

Report combined cadence, contact count and reference forward delta. Evaluate each emitted scenario against its own target and its own random-day baseline on the same evaluation dates. Do not compute an unqualified pooled hit or pooled lift over different events. Targets >=85% weeks with1–2 combined contacts and separate lift>=1.3 are acceptance checks, not guaranteed constraints or reasons to fill quotas.
