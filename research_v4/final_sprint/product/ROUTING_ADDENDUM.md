# Post-readout semantic routing diagnostic

Requested after the four joint-policy results were inspected. No new model fits or thresholds. Preserve the NOW-only contact schedule exactly. On a scheduled NOW date, route the copy and evaluation type to CLOSING only when the separate CLOSING head also exceeds its unchanged past-calibrated threshold and current observed ret1>0. Otherwise retain NOW.

This is dual-head confirmation on an existing NOW opportunity, not filling gaps with CLOSING. The CLOSING probability remains the CLOSING model's value. Evaluate routed contacts with the CLOSING endpoint label and remaining NOW contacts with the NOW future-minimum label. Shared cadence and dates must exactly match NOW-only. Report counts and uncertainty; small subsets do not demonstrate a scalable second scenario. The rule was introduced after2026 was viewed and is explicitly retrospective even though no2026 threshold tuning is performed.

Final semantic clarification after that exclusive-routing readout: NOW and CLOSING truth conditions need not be mutually exclusive. The additional `dual_annotations` profile preserves NOW on every original contact and adds CLOSING only as a secondary same-day tag when its own model passes the same threshold and ret1>0. Each tag retains its own score, target and denominator. It produces no new contacts, does not move dates, and does not remove strong observations from NOW evaluation. Secondary-tag counts must not be summed with primary contacts as independent messages. The 2025 delayed CLOSING failure and post-readout origin remain explicit; no all-years robustness claim follows from the2026 point estimates.
